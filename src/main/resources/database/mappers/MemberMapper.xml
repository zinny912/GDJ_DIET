<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goody.diet.member.MemberDAO">
	<select id="setMemberLogin" parameterType="MemberDTO" resultType="MemberDTO">
		SELECT * FROM "MEMBER" WHERE ID=#{id} AND PW=#{pw}
	</select>
	
	<insert id="setMemberJoin" parameterType="MemberDTO">
		INSERT INTO "MEMBER" (ID, PW, NAMES, PHONE, EMAIL, ADDRESS) 
		VALUES (#{id}, #{pw}, #{names}, #{phone}, #{email}, #{address})
	</insert>
	
</mapper>