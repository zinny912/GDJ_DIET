<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.goody.diet.healthMachine.HealthMachineDAO">
	
	
	<select id="getHealthMachineList" resultType="HealthMachineDTO">
	select * from healthmachine
	</select>
	<select id="getHealthMachineDetail" parameterType="HealthMachineDTO" resultType="HealthMachineDTO">
	select * from healthmachine WHERE machinenum=#{machineNum}
	</select>
	
	
	
	<!-- 옵션 -->
	<select id="getOption1" parameterType="RealHealthMachineDTO" resultType="RealHealthMachineDTO">
	SELECT DISTINCT OPT1NAME,OPT1ID FROM REALHEALTHMACHINE 
		WHERE MACHINENUM = #{machineNum}
	</select>
	<select id="getOption2" parameterType="RealHealthMachineDTO" resultType="RealHealthMachineDTO">
	SELECT DISTINCT OPT2NAME,OPT2ID FROM REALHEALTHMACHINE 
		WHERE MACHINENUM = #{machineNum} and OPT1ID=#{opt1Id}
		</select>
	<select id="getOption3" parameterType="RealHealthMachineDTO" resultType="RealHealthMachineDTO">
		SELECT DISTINCT OPT3NAME,OPT3ID FROM REALHEALTHMACHINE 
		WHERE MACHINENUM = #{machineNum} and OPT1ID=#{opt1Id}and OPT2ID=#{opt2Id}
		
		</select>
		<select id="getOption4" parameterType="RealHealthMachineDTO" resultType="RealHealthMachineDTO">
	SELECT DISTINCT OPT4NAME,OPT4ID FROM REALHEALTHMACHINE 
		WHERE MACHINENUM = #{machineNum} and OPT1ID=#{opt1Id}and OPT2ID=#{opt2Id}and OPT3ID=#{opt3Id}
		</select>
		<!-- 옵션 -->
</mapper>